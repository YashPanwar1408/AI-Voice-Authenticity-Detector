# Backend (FastAPI) runtime deps

# Model inference
numpy
pillow

# TensorFlow runtime (large wheel; Railway build is hardened via `nixpacks.toml` retries/timeouts)
tensorflow==2.21.0

# Audio conversion (bundled ffmpeg binary)
imageio-ffmpeg==0.6.0

# Backend API
fastapi
uvicorn
python-multipart
