streamlit==1.55.0

# TensorFlow is not available for Python 3.14+; Streamlit Cloud is forced to 3.12 via runtime.txt
tensorflow==2.21.0

numpy==2.4.3
pillow==12.1.1

# Bundled ffmpeg binary for audio conversion (no system ffmpeg required)
imageio-ffmpeg==0.6.0

# Training/data-prep only (DON'T install on Streamlit Cloud):
# - librosa
# - matplotlib
# - soundfile