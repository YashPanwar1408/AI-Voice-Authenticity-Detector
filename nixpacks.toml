[variables]
PIP_DISABLE_PIP_VERSION_CHECK = "1"
PIP_NO_CACHE_DIR = "1"
PIP_DEFAULT_TIMEOUT = "120"
PIP_PROGRESS_BAR = "off"

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install --no-cache-dir --retries 10 --timeout 120 -r requirements-api.txt",
]
